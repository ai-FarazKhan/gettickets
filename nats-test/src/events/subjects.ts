// enum is somewhat similar to an object, we going to list out couple of properties inside here, and we are going to then say, if somethings is of type subjects then its means that its one of the different properties listed out inside.
// so inside the Subjects we gonna list all the possible subjects we expect to have.
export enum Subjects {
    TicketCreated = 'ticket:created',
    OrderUpdated = 'order:updated'
}